<nav class="navbar">
  <% if current_user %>
    <p class="welcome" >WELCOME <span class="user"><%= current_user.name.upcase %></span></p>
  <% end %>
  <% if user_signed_in? %>
    <%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' }, class: "nav-link" %>
  <% end %>
</nav>