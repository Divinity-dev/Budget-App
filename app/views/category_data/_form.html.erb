<%= form_with(model: category_datum) do |form| %>
  <% if category_datum.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_datum.errors.count, "error") %> prohibited this category_datum from being saved:</h2>

      <ul>
        <% category_datum.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :data_id, style: "display: block" %>
    <%= form.text_field :data_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
